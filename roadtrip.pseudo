start = 0;
end = n;
x = array[n]; //where x[i] is the distance between hotel i-1 and hotel i
maxmiles = d;
function greedyDriving(x,start,end, maxmiles){
  hotels[];
  while (start != end){
    milestravelled = 0;
    for (i = start; milestravelled < maxmiles && i < end; i++){
      if (x[i] + milestravelled <= maxmiles){
        milestravelled += x[i];
      }
    }
    hotels.add(i);
  }
}
